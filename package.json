{
  "name": "uf-sase-website",
  "version": "1.0.0",
  "scripts": {
    "client-install": "cd client && npm install",
    "client-build": "cd client && npm run build",
    "server-install": "cd server && npm install",
    "start": "cd server && npm start",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm run client-install && npm run client-build && npm run server-install"
  },
  "engines": {
    "node": "20.x",
    "npm": "10.x"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "express": "^4.21.0",
    "react-router-dom": "^6.26.2",
    "vite": "^5.4.7"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1"
  }
}
